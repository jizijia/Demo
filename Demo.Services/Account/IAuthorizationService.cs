using System;
using System.Web;
using System.Web.Security;
using Demo.Core.Domain;
namespace Demo.Service
{
    public interface IAuthorizationService
    {
        //User CurrentUser { get; }
        //bool IsAuthored(int userId, string controller, string action);
        //bool IsAuthored(string controller, string action);
        //bool Login(string userName, string password, out string message, bool persistCookie = false);
        //void Logout();
        //User GetAuthenticatedUser();
        //User GetAuthenticatedUserFromTicket(FormsAuthenticationTicket ticket);
    }
}
